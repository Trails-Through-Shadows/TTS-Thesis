[
  {
    // 1) header example
    "alg": "HS256",
    "typ": "JWT"
  },

  {
    // 2) payload example
    "sub": "1234567890",
    "name": "John Doe",
    "admin": true
  }
]

// 3) Signature with HMAC SHA256
// HMACSHA256( base64UrlEncode(header) + "." + base64UrlEncode(payload), secret)
